<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:CounterKB.Models"
             x:Class="CounterKB.Views.AllCountersPage"
             x:DataType="models:AllCounters"
             Title="Counters">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Dodaj"
                     Clicked="Add_Clicked" />
    </ContentPage.ToolbarItems>

    <CollectionView ItemsSource="{Binding Counters}"
                    Margin="12"
                    SelectionMode="None">

        <CollectionView.ItemsLayout>
            <LinearItemsLayout Orientation="Vertical" ItemSpacing="8" />
        </CollectionView.ItemsLayout>

        <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="models:Counter">
                <Frame CornerRadius="8" Padding="10" HasShadow="True">
                    <Grid ColumnDefinitions="*,Auto,Auto">

                        <Label Grid.Column="0"
                               Text="{Binding Name}"
                               FontSize="18"
                               VerticalOptions="Center" />

                        <Label Grid.Column="1"
                               Text="{Binding Value}"
                               FontSize="20"
                               Margin="10,0" 
                               VerticalOptions="Center" />

                        <HorizontalStackLayout Grid.Column="2"
                                               Spacing="4"
                                               VerticalOptions="Center">
                            <Button Text="-" WidthRequest="48" Clicked="Decrease_Clicked" />
                            <Button Text="+" WidthRequest="48" Clicked="Increase_Clicked" />
                        </HorizontalStackLayout>

                    </Grid>
                </Frame>
            </DataTemplate>
        </CollectionView.ItemTemplate>

    </CollectionView>

</ContentPage>
